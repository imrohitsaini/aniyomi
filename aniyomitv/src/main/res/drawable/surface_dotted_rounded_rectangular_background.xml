<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fill color -->
    <solid android:color="@color/anime_tv_surface" />

    <!-- Rounded corners (tweak as you like) -->
    <corners android:radius="@dimen/chips_radius" />

    <!-- Dotted/Dashed stroke -->
    <stroke
        android:width="@dimen/_2dp"
        android:color="@color/anime_tv_onSurface"
        android:dashWidth="@dimen/_6dp"
        android:dashGap="@dimen/_4dp" />
</shape>
