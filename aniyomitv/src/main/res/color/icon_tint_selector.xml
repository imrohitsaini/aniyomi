<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected -->
    <item android:color="@color/anime_tv_primary" android:state_selected="true"/>

    <!-- Focused -->
    <item android:color="@color/soft_white" android:state_focused="true"/>

    <!-- Default -->
    <item android:color="@color/grey"/>
</selector>
