<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:background="@drawable/chips_focus_selector"
    android:padding="@dimen/_8dp">

    <TextView
        android:id="@+id/tvRepo"
        style="@style/AnimeTV_Text_Base"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/_16dp"
        android:gravity="center"
        android:padding="@dimen/_8dp"
        android:text="@string/add_repo"
        android:textColor="@color/anime_tv_onSurface"
        android:textSize="@dimen/_16sp"
        app:layout_constraintBottom_toBottomOf="@id/ivAddRepo"
        app:layout_constraintEnd_toStartOf="@id/ivAddRepo"
        app:layout_constraintHorizontal_chainStyle="packed"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/ivAddRepo" />

    <ImageView
        android:id="@+id/ivAddRepo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/svg_add"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/tvRepo"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
